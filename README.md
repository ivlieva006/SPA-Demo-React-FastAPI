# 🌐 SPA Demo — React + FastAPI

Полнофункциональное SPA-приложение с авторизацией JWT, базой данных и загрузкой данных с сервера.

---

## 🚀 Быстрый запуск

### 1. Клонировать проект
```bash
git clone git clone https://github.com/ivlieva006/SPA-Demo-React-FastAPI.git
cd spa-final
```

### 2. Запустить Backend
```bash
cd backend
cp .env.example .env
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```
Бекенд поднимется на **http://127.0.0.1:8000**

### 3. Запустить Frontend
В другом терминале:
```bash
cd frontend
cp .env.example .env
npm install
npm start
```
Фронт запустится на **http://localhost:3000**

---

## ⚙️ Технологии
- **Frontend:** React, React Router, Bootstrap  
- **Backend:** FastAPI, SQLAlchemy, JWT  
- **БД:** SQLite (по умолчанию, можно подключить PostgreSQL)
- **Auth:** JWT (регистрация, логин, logout, защищённые маршруты)

---

## 🧱 Структура проекта
```
spa-final/
├── backend/
│   ├── main.py
│   ├── models.py
│   ├── schemas.py
│   ├── database.py
│   ├── requirements.txt
│   ├── .env.example
│   └── app.db
│
└── frontend/
    ├── src/
    │   ├── components/
    │   ├── pages/
    │   ├── context/
    │   ├── services/
    │   ├── App.js
    │   └── index.js
    ├── package.json
    └── .env.example
```

---

## 🧩 Основные возможности
✅ Регистрация и авторизация пользователей через JWT  
✅ Загрузка и обработка данных с сервера (API)  
✅ Управление состоянием с помощью useState / useEffect  
✅ React Router для маршрутизации  
✅ Защищённые маршруты (PrivateRoute)  
✅ Возможность выхода из системы  
✅ Стилизация через Bootstrap и кастомный CSS  
✅ Хранение данных пользователей в базе  

---

